import { styled } from '@mui/material';

export const Table = styled('div')({
	background: '#c7b2b1',
	width: 250,
	height: 250,
	borderRadius: 10,
});

const PlayTable = () => {
	return <Table></Table>;
};

export default PlayTable;
